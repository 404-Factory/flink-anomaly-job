# Mark Lombok-generated methods (getters/setters/equals/hashCode/builders) with
# @lombok.Generated. JaCoCo automatically excludes @Generated members from coverage,
# so the coverage gate measures hand-written logic only — not boilerplate.
lombok.addLombokGeneratedAnnotation = true
config.stopBubbling = true
